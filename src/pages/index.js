export { default as NotFound } from "./404";
export { default as Login } from "./login";
export { default as SignUp } from "./signup";
export { default as User } from "./user";

// All the pages are imported here from each files, and can be used anywhere in tbe file, i think we import them into the App.jsx and provide the
